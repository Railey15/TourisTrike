// Server-side development recipient list. Add the actual approved addresses here.
// Never use example addresses: emergency photos/locations are sensitive.
// Existing tourist emergency contacts and tourism-office recipients are retained.
export const emergencyEmails: readonly string[] = [];
