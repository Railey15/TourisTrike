// Keep existing profile navigation compatible with the shared center.
import '../../shared/notification_center_screen.dart';

class NotificationsScreen extends NotificationCenterScreen {
  const NotificationsScreen({super.key});
}
